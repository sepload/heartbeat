# 无人机心跳监测可视化

基于 Streamlit 实现的无人机通信“心跳”监测系统，模拟心跳包发送、自动掉线检测并提供交互式可视化。

## 功能
- 模拟无人机每3秒发送一次心跳包（含序号和时间）
- 随机模拟10~20秒的掉线场景
- 自动检测超时（默认5秒）并标记掉线事件
- 动态展示心跳间隔变化曲线和掉线恢复点
- 可调节模拟时长、心跳间隔和掉线阈值

## 运行方式

### 本地运行
```bash
pip install -r requirements.txt
streamlit run app.py
